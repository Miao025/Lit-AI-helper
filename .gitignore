# ---------- OS / Editor ----------
.DS_Store
Thumbs.db
*.swp

# Visual Studio / VS Code
.vs/
.vscode/
*.user
*.suo
*.userosscache
*.sln.docstates

# ---------- .NET build outputs ----------
**/bin/
**/obj/
*.pdb
*.cache

# ---------- Test results ----------
TestResults/
coverage*/
*.trx

# ---------- NuGet ----------
# (通常不需要提交 packages，restore 会自动下载)
packages/
*.nupkg

# ---------- App local data / runtime artifacts ----------
# SQLite 数据库（如果你本地运行会生成）
*.db
*.db-shm
*.db-wal

# 日志/临时文件
*.log
logs/
tmp/
temp/

# ---------- Model files (避免把大权重提交进仓库) ----------
# 你现在的代码在 src/App.Infrastructure/Models 下，
# 但真正的模型权重通常会下载到某个 data/models 目录。
# 如果未来你决定把权重放在 repo 内某处，建议单独用 LFS 或外部下载。
**/models/**
**/Models/*.onnx
**/Models/*.bin
**/Models/*.pt
**/Models/*.safetensors

# ---------- Avalonia / UI (如果生成缓存) ----------
# (一般不多，但先留着防止生成临时资源)
**/*.axaml.cs

# ---------- Build artifacts / packaging outputs ----------
# 你仓库里的 build/ci 和 build/packaging 是配置脚本，建议提交；
# 但如果将来生成 dist/artifacts/output 这类产物，建议忽略
dist/
artifacts/
out/
output/
publish/

# ---------- Python tooling (tools/model-export 可能用到) ----------
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
